# Product Spec

## 产品概述

VrLauncher 是一个运行在 Windows 上的轻量启动工具，用卡片化方式统一管理本地应用和网页入口。用户把常用 `.exe`、`.bat`、`.cmd` 或网页链接添加进来后，可以在一个面板里快速启动、筛选、排序、编辑和删除这些入口，而不是每次去桌面、开始菜单、收藏夹或一堆浏览器标签里翻。

**目标用户**不是“所有人”，这种话没有意义。更准确地说，是这几类 Windows 高频用户：
- 经常在多个本地工具之间切换的办公用户
- 同时要打开内网页面、后台页面、管理页面的运维/开发用户
- 有固定启动组合的软件重度用户
- 想把常用程序和网页收拢到一个统一入口的人

**核心价值**：
- 把分散的应用和网页入口收拢成一个清晰可见的卡片库
- 降低“找入口”和“重复打开”的时间浪费
- 让启动动作从“去系统里找”变成“在一个面板里点一下”
- 为后续加入更强的启动编排、分组和自动化能力留出基础

## 应用场景

- **办公用户上班启动固定工具**：用户每天都要打开企业微信、浏览器后台、报表网页、本地脚本工具。打开 VrLauncher 后，直接点击对应卡片，省掉在桌面和收藏夹里来回找。
- **开发/运维人员进入工作状态**：用户需要同时启动本地客户端、测试工具、内网页面和批处理脚本。VrLauncher 作为统一入口，减少漏开和误开。
- **游戏或内容创作者准备工作环境**：用户会反复打开录屏、剪辑、素材站、资源管理工具。卡片面板让这些入口固定可见，不再依赖记忆。
- **家庭电脑常用入口收纳**：用户把常用网站、播放器、聊天工具、启动脚本加入卡片库，电脑使用路径更短，也更直观。

## 功能需求

### 核心功能

- **添加应用卡片**：用户填写名称并选择本地程序或脚本路径 -> 系统校验路径是否存在，并保存为卡片 -> 用户得到一个可启动的应用入口。
- **添加网页卡片**：用户填写名称和网页链接，并可指定浏览器 -> 系统补全 `https://`、校验 URL、检查指定浏览器是否存在 -> 用户得到一个可打开网页的卡片入口。
- **展示卡片列表**：用户打开工具 -> 系统从本地配置读取全部入口并按顺序渲染为卡片 -> 用户看到统一的应用/网页面板。
- **启动本地应用**：用户点击应用卡片上的“启动”按钮，或双击卡片 -> 系统按应用类型启动 `.exe`、`.bat`、`.cmd`，并记录运行状态 -> 用户快速打开目标应用。
- **打开网页**：用户点击网页卡片上的“打开”按钮 -> 系统使用默认浏览器或指定浏览器打开该链接 -> 用户快速进入目标网页。
- **关闭本地应用**：用户点击卡片上的“关闭”按钮 -> 系统尝试结束该卡片对应的进程及其子进程 -> 用户无需再去任务管理器手动关闭。
- **编辑卡片信息**：用户修改名称、路径、图标、描述、工作目录或浏览器设置 -> 系统覆盖保存该卡片配置 -> 用户可以持续维护入口信息。
- **删除卡片**：用户选中卡片后执行删除 -> 系统移除本地配置中的对应项 -> 用户清理不再需要的入口。
- **排序卡片**：用户通过拖拽、上移、下移调整顺序 -> 系统更新本地配置中的顺序 -> 用户把常用项放在更顺手的位置。
- **搜索卡片**：用户输入关键词 -> 系统按名称和路径做前端过滤 -> 用户能更快定位目标入口。

### 辅助功能

- **选择应用文件**：用户在新增/编辑应用时点击选择文件 -> 系统调用本地文件选择器 -> 用户不用手输完整路径。
- **打开应用所在目录**：用户点击应用卡片上的文件夹按钮 -> 系统打开资源管理器并进入目标目录 -> 用户可以快速定位程序位置。
- **自定义卡片图标**：用户为卡片填写本地图标路径 -> 系统在支持图片格式时展示图标 -> 用户获得更直观的识别体验。
- **显示状态反馈**：用户执行启动、关闭、保存等操作 -> 系统显示“就绪 / 启动中 / 关闭中 / 错误”等状态 -> 用户知道操作有没有成功。
- **切换界面皮肤**：用户在设置中选择不同主题皮肤 -> 系统保存皮肤配置并即时应用 -> 用户可以调整视觉风格。
- **调整拖拽触发时长**：用户设置长按拖拽阈值 -> 系统保存交互参数 -> 用户可以降低误触或提升排序效率。

### 当前版本边界

- 第一版以**单机、本地配置、单用户使用**为前提，不做登录、云同步、多人协作。
- 第一版支持的网页浏览器以系统默认、Edge、Chrome、QQ 浏览器为主。
- 第一版重点解决“统一入口”和“快速启动/关闭应用”，不做复杂工作流编排。
- **网页一键关闭**是产品目标的一部分，但要加限制条件：只有由 VrLauncher 启动、且受控浏览器实例/窗口承载的网页，才有机会做可靠关闭。直接关闭“默认浏览器里某个现有标签页”这件事，第一版不应该承诺。

## UI 布局

### 整体布局

桌面单窗口布局，主体为上方工具栏加下方卡片区：
- 顶部为控制区，用于搜索、刷新、设置和全局操作
- 中部为卡片网格区，展示应用与网页入口
- 弹窗层用于新增/编辑卡片、设置、确认和提示

### 顶部控制区

- 左侧放产品标题与状态信息
- 中间放搜索框，支持按名称和路径过滤
- 右侧放操作按钮，如刷新、设置、新增入口

### 卡片网格区

- 采用网格方式排列卡片，每张卡片展示一个入口
- 卡片内容包括：
  - 图标区域
  - 名称
  - 路径或网址摘要
  - 操作按钮区
- 应用卡片额外提供“打开所在文件夹”入口
- 空状态下仍保留“添加应用”卡片，作为主要新增入口

### 卡片交互规范

- 单击卡片：选中
- 双击卡片：快速启动
- 点击主按钮：应用为“启动”，网页为“打开”
- 点击危险按钮：关闭对应应用；网页关闭能力需按受控窗口方案实现
- 长按并拖拽：调整卡片顺序
- 选中态：高亮边框/发光效果，明确当前操作对象

### 新增/编辑弹窗

- 支持两种类型切换：`应用` / `网页链接`
- 应用表单字段：
  - 名称
  - 应用路径
  - 工作目录
  - 图标路径
  - 描述
- 网页表单字段：
  - 名称
  - 网页链接
  - 浏览器选择
  - 图标路径
  - 描述

### 设置弹窗

- 主题皮肤选择
- 拖拽长按时长设置
- 保存和取消操作

## 用户使用流程

### 首次使用

1. 用户打开 VrLauncher。
2. 系统读取本地配置；若为空，则展示空卡片区和“添加应用”入口。
3. 用户点击新增入口。
4. 用户选择添加“应用”或“网页链接”。
5. 用户填写表单并保存。
6. 系统校验并写入本地配置。
7. 新卡片出现在卡片网格中。
8. 用户点击卡片完成启动或打开。

### 日常使用

1. 用户打开工具。
2. 系统展示上次保存的全部卡片顺序和界面主题。
3. 用户通过搜索或直接点击卡片找到目标入口。
4. 用户启动应用或打开网页。
5. 用户在需要时关闭本地应用、编辑卡片或调整顺序。

### 配置维护

1. 用户选中已有卡片。
2. 用户编辑、删除、上移、下移或拖拽排序。
3. 系统实时更新本地配置文件。
4. 下次打开工具时保持相同结果。

## AI 能力需求

当前版本**不需要 AI 能力**。这个产品的核心问题是入口管理和启动控制，不是内容生成。别为了赶时髦硬塞 AI，把简单工具做复杂。

| 能力类型 | 用途说明 | 应用位置 |
|------|------|------|
| 无 | 当前版本不接入 AI，避免增加无必要复杂度 | 全产品 |

### 可选的后续 AI 增强方向

这些不是第一版必须做的东西，只是后续可以考虑：
- 根据用户使用频率推荐置顶卡片
- 根据历史启动习惯推荐“工作场景组合”
- 自动识别拖入路径并补全名称、图标、描述

## 技术方向

| 维度 | 选择 | 理由 |
|------|------|------|
| 产品类型 | Desktop | 需要访问本地文件、启动本地程序、管理系统进程，桌面应用才是正路 |
| 推荐技术栈 | Python + pywebview + HTML/CSS/JavaScript | 当前项目已经采用这套方案，能较快交付 Windows 本地工具，改造成本最低 |
| 数据存储 | 本地 JSON | 第一版是单机工具，配置量小，本地存储足够且实现简单 |
| 部署方式 | Windows 安装包 / 可执行文件 | 符合桌面工具分发方式，用户安装后即可本地使用 |

## 技术说明

- **系统权限**：需要具备启动本地程序、结束进程、打开文件夹等本地系统操作能力。
- **进程管理**：本地应用关闭依赖进程记录和进程树清理，批处理脚本需要特殊处理。
- **浏览器兼容**：网页启动依赖系统默认浏览器或本地已安装的指定浏览器。
- **本地配置文件**：
  - `config/apps.json`：保存卡片列表与顺序
  - `config/ui.json`：保存皮肤和交互设置
- **打包发布**：当前项目已包含 `PyInstaller` 打包路径，适合继续沿用。

## 补充说明

### 卡片数据结构

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 卡片唯一标识 |
| name | string | 卡片名称 |
| path | string | 应用路径或网页 URL |
| working_dir | string | 应用工作目录，网页为空 |
| icon | string | 本地图标路径 |
| description | string | 卡片描述 |
| kind | enum(`app`,`url`) | 卡片类型 |
| url_browser | enum(`default`,`edge`,`chrome`,`qq`) | 网页使用的浏览器 |

### 第一版明确不做

- 账号体系
- 云端同步
- 多设备同步
- 浏览器标签级精确控制
- 应用分组/文件夹体系
- 批量启动场景编排
- 使用统计与智能推荐

### 待补充项

- [待补充] 是否需要开机自启
- [待补充] 是否需要最小化到系统托盘
- [待补充] 是否需要导入/导出配置
- [待补充] 网页“一键关闭”的受控实现方案（独立窗口、专用浏览器实例、或嵌入式 WebView）
